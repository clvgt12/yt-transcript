# yt-transcribe-web Python dependencies
#
# PyTorch pinned to 2.2.0+cu118 — last build supporting NVIDIA Pascal (sm_61 / GTX 1050 Ti)
# numpy pinned below 2.0 — PyTorch 2.2.x compiled against NumPy 1.x C API

numpy<2
openai-whisper
streamlit>=1.35.0
requests>=2.31.0
markdown>=3.5.0
