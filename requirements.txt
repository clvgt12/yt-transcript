# yt-transcribe-web Python dependencies — PyPI packages
# PyTorch installed separately in Dockerfile from pytorch.org wheel index
# numpy pinned below 2.0 — PyTorch 2.2.x compiled against NumPy 1.x C API

numpy<2
openai-whisper
streamlit>=1.35.0
requests>=2.31.0
markdown>=3.5.0
duckduckgo-search>=6.0.0
